<%= div_for event, :class => 'content_size' do %>
  Invites to <%=link_to(event.name, event)%> </br>
  <% if !event.initDate.blank? %>
    Since  <%= event.initDate.gmtime.to_s %>
  <% end %>
  <% if !event.endDate.blank? %>
    until <%= event.endDate.gmtime.to_s %>
  <% end %>

<% end %>