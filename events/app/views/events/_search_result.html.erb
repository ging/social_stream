<div class="event">
  <div class="avatar">
    <%= link_to icon(event, 50), event, :title => event.title %>
  </div>

  <div class="data">
    <div class="name">
      <%= link_to truncate(event.title, :length => 15), event %>
    </div>

    <div class="date">
      <%= event.start_at.to_formatted_s(:rfc822) %>
    </div>

    <div class="frequency">
      <%= event.frequency_description %>
    </div>
  </div>
</div>
