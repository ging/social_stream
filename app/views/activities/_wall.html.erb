<%= render :partial => "activities/activities",
           :locals  => { :owner      => parent,
                         :activities => parent.wall(:profile,
                                                    :for      => current_subject,
                                                    :relation => (params[:section].present? ? params[:section].to_i : nil)) } %>

