<% content_for :headers do %>
<%= stylesheet_link_tag "messages.css", :media => "screen, projection" %>
<% end %>

<%= render :partial => 'location' %>
<br class="clearfloat" />
<div class="space_center">
</div>
<div id="new_message">
	<% if @box.present? %>
    <%= link_to 'Back', messages_path(:box => @box, :page => params[:page]), :remote => true %>
	<% else %>
    <%= link_to 'Back', messages_path(:page => params[:page]), :remote => true %>
	<% end %>
</div>
<div class="space_center">
</div>
<div class="conversation_full">  
 <%= render :partial => 'conversation_full', :locals =>{:conversation => @conversation} %>
</div>
