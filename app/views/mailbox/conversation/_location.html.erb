<div id="map_location" class="content_size">
  <%= t('location.message', :location => "#{ image_tag('btn/btn_inbox.png' , :class => 'btn_config') } #{ t('inbox.one') }: <span class=\"name_group\">#{ sanitize @conversation.subject }</span>").html_safe %>
</div>
