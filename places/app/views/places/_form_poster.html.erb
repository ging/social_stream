<div>
	<%= form_for @place.poster do |f| %>
	  <%= f.hidden_field :place_property_object_id %>
	  <%= f.hidden_field :owner_id %>

	  <%= f.file_field :file %>

	  <%= f.submit t('place.poster.button') %>
	<% end %>
</div>
