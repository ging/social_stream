<div id="avatar_profile">
  <% if subject == current_subject %>

    <%= link_to image_tag(subject.logo.url(:profile), :alt => subject.name , :size => "150x150", :id => "current_avatar_img"), subject %>    
    <div class="logo_menu logo_caption">    
      <%= link_to t('avatar.profile_change'), avatars_path  %>       
    </div>        
  <% else %>
    <%= link_to image_tag(subject.logo.url, :alt => subject.name , :size => "150x150"), subject %>
    <%= subject.name %>
  <% end %>
</div>
