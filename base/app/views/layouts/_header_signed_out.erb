<header class="headerFront <%= header_logo_class %>">
  <%= render partial: 'layouts/logo-out' %>
  <div class="brand">
  </div>
  <% unless params[:controller].eql? 'devise/sessions' and params[:action].eql? 'new' %> 
  <% end %>
</header>
