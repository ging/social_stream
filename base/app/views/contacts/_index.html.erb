<%=
  if d == params[:d]
    contacts = @contacts

    if params[:d] == 'received'
      contacts = Kaminari.paginate_array(contacts.map{ |c| c.inverse! })
    end

    render contacts.
      page(params[:page]).
      per(20)
  else
    image_tag 'loading.gif'
  end
%>
