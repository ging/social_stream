<%= form_for contact, remote: true, html: { 'data-contact_id' => contact.id, 'data-relation_count' => contact.options_for_select.count } do |f| %>
  <%= f.select :relation_ids, contact.options_for_select, {}, { multiple: true } %>
<% end %>

